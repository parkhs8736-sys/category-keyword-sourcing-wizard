# 카테고리 키워드 소싱판별 마법사 — Codex 인수인계서

> 다른 Codex 프로젝트에 이 문서의 링크 또는 전체 내용을 붙여 넣고, **"이 인수인계서를 기준으로 기존 동작을 보존하며 작업을 이어가 주세요"**라고 요청하면 됩니다.

## 1. 프로젝트 한눈에 보기

- **프로젝트명:** 카테고리 키워드 소싱판별 마법사 1.0
- **로컬 실행 폴더:** `sourcing-judge-admin`
- **로컬 실행 주소:** `http://localhost:8787/`
- **공개 GitHub 저장소:** <https://github.com/parkhs8736-sys/category-keyword-sourcing-wizard>
- **현재 공유 브랜치:** `agent/github-share-v05`
- **검토 PR:** <https://github.com/parkhs8736-sys/category-keyword-sourcing-wizard/pull/1>
- **주 언어/구성:** HTML, CSS, Vanilla JavaScript, Node.js 로컬 서버, Chrome 확장프로그램

이 프로그램은 셀러라이프 카테고리 키워드 엑셀을 업로드하고 조건으로 필터링한 뒤, `https://select.irumai.kr/` 결과를 이용해 소싱 통과 여부를 정리합니다. 같은 화면 아래쪽에는 **윙렌즈(Chrome 확장프로그램)** 를 통한 쿠팡 판매지표 분석도 통합되어 있습니다.

## 2. 현재 제공 기능

### 소싱판별

1. 엑셀 업로드 후 B열 키워드를 읽습니다. 첫 행 제목은 자동 제외합니다.
2. 엑셀 필터링 조건을 적용합니다. 브랜드/쇼핑성/검색량/가격/배송비율/리뷰수/계절성 등을 지원합니다.
3. 필터 결과를 50·100·200·300·500개 단위로 표기하고, 표시할 엑셀 열을 사용자가 체크해 저장할 수 있습니다.
4. 선택된 키워드를 소싱판별 실행으로 넘기며, 결과(통과여부·사유·카테고리·경쟁률 등)를 표로 확인하고 엑셀로 내려받습니다.
5. 필터 설정과 판별결과 목록, 각 접기/펼치기 상태는 브라우저 로컬 저장소에 보관합니다.

### 쿠팡 판매지표 분석(윙렌즈 연동)

1. `wing-lens-extension`을 Chrome 확장프로그램으로 압축해제 로드합니다.
2. 쿠팡 검색 결과와 로그인된 쿠팡윙 세션을 사용해 가격, 리뷰, 배송, 최근 28일 PV·판매량·매출·전환율을 표시합니다.
3. 배송유형(해외/로켓/설치/국내)과 리뷰 수 조건으로 대상 상품을 거를 수 있습니다.
4. 소싱판별 결과 또는 예약 목록에서 체크한 키워드만 **선택 키워드 조회**로 즉시 분석합니다.
5. 예약 목록은 브라우저 재시작 뒤에도 복원됩니다. 예약리스트 삭제는 **체크한 예약 항목만** 삭제합니다.
6. 쿠팡의 403/429 또는 접근 제한을 감지하면 원격 요청을 멈추고, 로컬 회로 차단기와 체크포인트로 안전하게 중단 상태를 유지합니다.

## 3. 반드시 지켜야 할 운영 원칙

- 쿠팡의 CAPTCHA, 접근 제한, 로그인 정책을 우회하는 기능을 만들지 않습니다.
- 로그인 ID·비밀번호·쿠키·세션 토큰·메일 앱 비밀번호를 코드, Git, 로그, 화면에 저장하거나 노출하지 않습니다.
- 접속 제한이 발생하면 자동 반복 요청을 하지 않습니다. 사용자가 직접 상태를 확인한 뒤 선택 키워드 조회를 실행합니다.
- 예약된 키워드는 자동으로 조회하지 않습니다. 사용자가 체크하고 `선택 키워드 조회`를 눌렀을 때만 실행합니다.
- `선택 키워드 조회`는 체크한 예약 ID만 처리해야 합니다. 체크하지 않은 대기/재개대기 키워드를 함께 실행하면 안 됩니다.
- 기존 사용자의 브라우저 로컬 저장값(필터, 목록, 접힘 상태)을 임의로 삭제하거나 초기화하지 않습니다.

## 4. 핵심 파일 구조

| 경로 | 역할 |
|---|---|
| `index.html` | 통합 관리자 화면의 HTML 구조 |
| `app.js` | 엑셀 업로드·필터·소싱판별·예약·윙렌즈 화면 연동의 핵심 로직 |
| `style.css` | 전체 관리자 화면과 윙렌즈 통합 화면 스타일 |
| `filters.css` | 필터 UI 전용 스타일 |
| `target-list.js` | 필터 결과 목록, 열 선택, 정렬, 접기/펼치기 |
| `filter-preset-auto.js` | 저장 필터 선택 시 자동 불러오기 |
| `server.mjs` | 로컬 Node 서버, 메일 발송, 확장프로그램 파일 제공 |
| `실행하기.bat` | 로컬 서버 실행 및 브라우저 열기 |
| `메일설정하기.ps1` | Gmail 앱 비밀번호를 사용자 환경변수에 저장하는 보조 스크립트 |
| `tests/wing-safety.test.mjs` | 윙렌즈 제한 대응·선택 조회 범위 등 안전 동작 검사 |
| `wing-lens-extension/` | Chrome 확장프로그램 전체 소스 |
| `VERSION_HISTORY.md` | 최근 UI/기능 변경 기록 |
| `previews/ui-redesign-v0.1.html` | UI 미리보기용 정적 화면. 기능 테스트 대상이 아님 |

## 5. 실행·검증 방법

### 로컬 실행

1. Node.js 18 이상을 설치합니다.
2. 프로젝트 폴더에서 `실행하기.bat`를 더블클릭합니다.
3. 실제 기능은 `http://localhost:8787/`에서 확인합니다.

`previews/ui-redesign-v0.1.html`은 정적 디자인 미리보기라 버튼 동작을 검증할 수 없습니다.

### 검사 명령

```powershell
cd "C:\Users\USER\OneDrive\문서\CODEX\카테고리소싱\sourcing-judge-admin"
node --check app.js
npm test
```

현재 자동 테스트는 `tests/wing-safety.test.mjs`를 사용합니다. 기능을 바꿀 때에는 관련 테스트를 추가하거나 수정한 뒤 `npm test`를 통과시켜야 합니다.

## 6. GitHub 공유 범위

다음 항목은 공개 저장소에서 제외되어야 합니다.

- `auth-store.json`
- `.env`, `.env.*`
- `node_modules/`
- 개인 엑셀 파일(`*.xlsx`)
- 이메일 앱 비밀번호, 쿠팡 로그인 정보, 브라우저 인증 상태

`.gitignore`에 위 제외 규칙이 들어 있습니다. 공개 저장소에 들어간 화면용 비밀번호는 보안 수단이 아니므로, 중요한 접근 제어는 서버 측 인증으로 별도 구현해야 합니다.

## 7. 최근 개발 상태

- **v0.5 (2026-07-31):** 예약리스트 삭제를 선택 항목 삭제로 변경했습니다. 실행 중에는 먼저 중단한 뒤 삭제하도록 보호합니다.
- **v0.4:** 선택 키워드 조회가 체크한 예약 ID만 실행하도록 범위를 제한했습니다.
- **v0.3:** 사용자가 직접 선택 조회를 누를 때에만 로컬 차단기 해제 후 재시도를 허용합니다.
- **v0.2:** 예약일시 자동 실행과 90초/180초 자동 대기를 제거했습니다.
- 자세한 내용은 `VERSION_HISTORY.md`를 먼저 읽습니다.

## 8. 다음 Codex에게 전달할 작업 규칙

1. 작업 전 `git status -sb`, 관련 파일, `VERSION_HISTORY.md`를 확인합니다.
2. 사용자 요청 범위 밖의 UI·동작·데이터를 임의로 삭제하지 않습니다.
3. 변경은 작게 나누고 `apply_patch`로 수정합니다.
4. 기능 변경 후 `node --check app.js`와 `npm test`를 실행합니다.
5. GitHub 커밋/푸시는 사용자가 명확히 요청했을 때만 합니다. 개인 파일·비밀정보는 절대 스테이징하지 않습니다.
6. 결과 보고는 한국어로, 변경 파일·검증 결과·사용자가 할 일을 짧게 알립니다.
