# UI 버전 기록

## v0.5 — 2026-07-31

- `예약리스트 삭제`는 전체 목록이 아니라 체크한 예약 키워드만 삭제하도록 변경했다.
- 실행 중인 예약 목록은 작업 상태 보호를 위해 먼저 `중단`한 뒤 삭제할 수 있다.

변경 파일: `app.js`, `tests/wing-safety.test.mjs`.

## v0.4 — 2026-07-31

- 판매지표 분석예약 목록에 `예약리스트 삭제` 버튼과 확인창을 추가했다. 삭제를 확정하면 진행 중인 작업도 중단하고, 브라우저에 저장된 예약 목록·체크포인트를 함께 정리한다.
- `선택 키워드 조회`는 체크한 예약 항목의 ID만 실행하도록 범위를 제한했다. 체크하지 않은 대기·재개대기 항목은 이번 실행에서 조회하지 않는다.

변경 파일: `app.js`, `tests/wing-safety.test.mjs`.

## v0.3 — 2026-07-30

- 사용자가 판매지표 분석예약 목록에서 키워드를 체크한 뒤 `선택 키워드 조회`를 누르면, 이전 조회 오류로 열린 로컬 차단기를 해제하고 즉시 한 번 재조회한다.
- 자동 재시도·자동 예약 실행은 여전히 사용하지 않는다. 실제 쿠팡 오류가 다시 발생하면 해당 응답에 따라 다시 중단된다.

변경 파일: `app.js`, `tests/wing-safety.test.mjs`.

## v0.2 — 2026-07-30

- 판매지표 분석예약 목록에서 사용자가 체크한 뒤 `선택 키워드 조회`를 누르면 예약일시와 무관하게 즉시 조회한다.
- 키워드가 여러 개인 경우 기존의 90초·180초 자동 대기 없이 다음 선택 키워드를 바로 시작한다.
- 예약일시 도래를 감지해 자동으로 조회를 시작하던 기능을 제거했다.
- 쿠팡의 실제 접근 제한(403/429)이 감지된 경우의 보호 중단은 유지한다.

변경 파일: `app.js`, `tests/wing-safety.test.mjs`.

## v0.1 — 2026-07-30

- 엑셀파일올리기 제목의 음영·배경 효과를 제거했다.
- 다운로드방법 안내 버튼을 파일 선택 영역 아래줄 오른쪽으로 옮겼다.
- 키워드 필터링, 필터결과 목록, 소싱판별의 접힘 버튼 이름을 `접기`·`펼치기`로 통일했다.
- 세 버튼을 각각의 제목 바로 옆으로 옮겼다.

변경 파일: `app.js`, `style.css`, `target-list.js`.

이 버전으로 되돌리기 요청이 있으면 이 기록을 기준으로 복원한다.
